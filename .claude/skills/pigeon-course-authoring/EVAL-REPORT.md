# 忠实度门禁评测报告 · verify-fidelity.mjs

**一句结论:** 门禁在真实规模课程上行为正确 —— 忠实源全部放行(EVAL 1 退出 0)、单字改动被精确抓出(EVAL 2 退出 1),正文文本对真实外部源零误报(EVAL 3 正文 1044 块 0 漏)。

## 结果表

| 评测 | 预期 | 实际 | 通过 |
|------|------|------|------|
| EVAL 1 派生忠实源(scale + 全块型) | 退出 0,无漏报 | 退出 0;1564 块 / 有效 1520 块 / 0 漏报 | ✓ |
| EVAL 2 单字改动(连→联,概述 4.2 一段) | 退出 1,改动块被报出 | 退出 1;命中 1 块,正是被改的概述 4.2 段 | ✓ |
| EVAL 3 真实外部源 `参考项目/index.html` | 报告误报率 | 退出 1;67/1520 报出,**全为题库项,正文 0 报出** | ✓(见说明) |

## 关键数字

- **规模:** content.json + quiz.json 共抽取 **1564 块**,有效校验(去单字)**1520 块**(正文 1044 + 题库 476)。
- **EVAL 1 用时:** `real 0m0.118s`(冷启;整库子串比对在百毫秒级,无崩溃、无误报)。
- **EVAL 2:** 仅报 1 块,预览即含被篡改的「占领微电子互**联**方面」,定位准确。
- **EVAL 3 误报构成(67 块):** 题干 25 + 选项 26 + 右项 5 + 左项 4;**正文类(段落/表/卡)误报 0**。
  - **正文误报率 0/1044 = 0%**;题库 67/476 ≈ 14.1%,但属"源不含该文本",非门禁逻辑误判。

## 说明 / Caveats

- EVAL 3 的 67 例非真·假阳性,而是**外部源选择所限**:`参考项目/index.html` 的题库文本存放在 `<script>` 内的 `var ALL_QUESTIONS / QUIZ_DATA` JS 字面量里,而脚本 `readSourceText()` 设计上会剥离 `<script>`,故题库文本不进比对池 → 在源中"找不到"。**正文(DOM 文本)100% 命中**,证明门禁对真实外部源处理正确。
- 同目录 `课程作业综合版.md` 含部分题面,但属另一份/不全文档(对它校验 924 块漏报),不适合作题库源。
- 题库的真正干净来源是 JS 字面量;按既定设计未给脚本加 `.js` 支持(也未改脚本)。
- 题库 explain 字段在作者环节确有润色(如"芯片减薄共有8种技术"→"…共8种技术"),但 explain 本就在门禁跳过范围内,不影响结论。
- 已清理全部临时目录;`courses/ic-packaging/` 工作区无改动(git 确认)。
